Implement R0 measurement validity as a single, narrowly scoped change.

Allowed project files: `.coord/runs/measure_p05.py`, `tests/test_r0_measurement_validity.py` only if additive production coverage is necessary, `.coord/runs/R0/p05-correction.json`, `.coord/tasks/R0-cost-measurement-validity.md`, and `.coord/PLAN.md`. Never modify `.coord/runs/P05/ab.json`.

The fixed acceptance is the ORIGINAL source file `tests/test_r0_measurement_validity.py`; do not edit it. Add `evaluate_measurement(evidence: dict) -> dict` to `measure_p05.py` and make the live runner use the same validator. Invalid results must have a concrete `INVALID_*` status and no `savings` field. A valid result may be `MEASURED_AND_VERIFIED` only when both Codex runs exited 0, forbidden infrastructure errors are absent, task and baseline/source hashes match, direct expected behavior passed, B pilot summary exists, summary task/run/source/bundle identity matches a terminal SQLite ledger, verdict is PASS, promotion is actually APPLIED, and post-apply acceptance exits 0. Compute task/start identity from evidence; never hardcode them. Compute total, noncached input, and wall reductions only after all gates. Tool event count and total test count are diagnostic only.

Write `.coord/runs/R0/p05-correction.json` preserving the raw P05 source path and classifying the historical B as `INVALID_SETUP`, its comparison as `INVALID_MEASUREMENT`, and savings as `UNMEASURED`. Update the R0 card/PLAN minimally while preserving M4 history. Run the fixed acceptance and relevant regression tests in staging.
