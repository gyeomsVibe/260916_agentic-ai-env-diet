In this workspace, edit only calc.py and test_calc.py.
1. Add to calc.py: `mean(values)` (ValueError on empty), `median(values)` (ValueError on empty; average of middle two for even length), and `percent_change(old, new)` (ValueError if old == 0; returns (new-old)/old*100). One-line docstring each.
2. Add unit tests to test_calc.py: at least 3 tests per new function including error cases.
Do not modify existing functions or tests. Do not create, delete, or rename files. Do not run shell commands. Reply with a one-line summary.
