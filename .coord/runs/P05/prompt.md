In this workspace, edit only calc.py and test_calc.py.
1. Add to calc.py: `power(base, exponent)` (returns base ** exponent) and `reciprocal(x)` (ValueError if x == 0; returns 1.0 / x). One-line docstring each. Do not change existing functions.
2. Add unit tests to test_calc.py: at least 3 tests per new function including the zero-input error for reciprocal.
Do not create, delete, or rename files. Reply with a one-line summary.
