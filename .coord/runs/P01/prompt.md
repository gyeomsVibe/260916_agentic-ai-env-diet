In this workspace, edit only calc.py and test_calc.py.
1. Add a function `clamp(value, low, high)` to calc.py that returns low if value < low, high if value > high, otherwise value. Raise ValueError if low > high. Include a one-line docstring.
2. Add unit tests to test_calc.py covering: inside range, below low, above high, equal to bounds, and low > high raises ValueError.
Do not create, delete, or rename any other file. Do not run shell commands. Reply with a one-line summary.
