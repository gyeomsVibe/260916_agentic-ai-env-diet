In this workspace, edit only calc.py and test_calc.py.
1. Add to calc.py: `variance(values)` (population variance; ValueError on empty) and `stdev(values)` (square root of variance; ValueError on empty). One-line docstring each. Do not change existing functions.
2. Add unit tests to test_calc.py: at least 3 tests per new function including the empty-input error.
Do not create, delete, or rename files. Do not run shell commands. Reply with a one-line summary.
