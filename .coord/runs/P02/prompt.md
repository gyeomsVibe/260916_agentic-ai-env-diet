In this workspace, edit only calc.py and test_calc.py.
1. Add a function `sub(a, b)` to calc.py that returns `a - b`. Include a one-line docstring and preserve the existing functions.
2. Add unit tests to test_calc.py covering positive, negative, and zero results.
3. Run `python -m unittest -v` and ensure all tests pass.
Do not create, delete, or rename any other file. Do not commit, push, or merge. Reply with a one-line summary.
