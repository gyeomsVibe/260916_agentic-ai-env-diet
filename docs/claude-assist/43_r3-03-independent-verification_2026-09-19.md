# Claude 독립 검증 43: R3 attempt 03 (2026-09-19 16:2x)

## 판정: 유효한 1쌍 측정으로 인정 (MEASURED_AND_VERIFIED, n=1)

| 확인 항목 | 결과 |
|---|---|
| 동일 과제·동일 시작 manifest | A·B 모두 `sha256:678c32…` |
| 품질 | A 12/12, B 12/12, 변경 파일 `calc.py`·`test_calc.py`만 |
| B 조율 1턴 실측 | `P05-R3-B-03-coordinator.jsonl`: exit 0, 도구 0, 답 `APPROVE 3282ea…` = receipt bundle과 일치 |
| 하드코딩 | 본 판정 경로 없음. 581행 `codex_exit: 0`은 R1 A를 쓰는 **참고 비교**(R1 A exit 0일 때만)로 본 판정과 분리 |
| 지표 분리 | 게이트에는 Codex 토큰만, Antigravity 토큰은 별도 참고 |
| 역사 기록 | `P05/ab.json` 해시 `f84487…` 불변 |

## 수치 (Codex 계정 기준)

| 지표 | A (Codex 단독) | B (Codex 조율 1턴 + Antigravity) | 차이 |
|---|---:|---:|---:|
| 입력 토큰 합계 | 70,433 | 18,548 | **−73.7%** |
| 그중 비캐시 입력 | 6,945 | 6,900 | −0.6% |
| 출력 토큰 | 755 | 46 | −93.9% |
| Codex 도구 호출 | 4 | 0 | — |
| 벽시계(초) | 60.6 | 56.7(pilot 40.5 + 조율 16.1) | −6.4% |

Antigravity 참고: B worker 입력 70,352, 캐시 읽기 146,707, 출력 3,457(gemini-3.7-flash-high).

## 해석 (주의)

- **Codex 쪽 부담은 확실히 줄었다**: 입력 합계 −74%, 출력 −94%, 도구 호출 0.
- 단, **비캐시 입력은 거의 같다**(−0.6%). Codex 사용량 한도가 캐시 입력을 얼마나 할인하는지에 따라 실제 한도 절감 폭은 달라진다. 캐시를 10%로 셈한다고 가정하면 A ≈ 13.3k, B ≈ 8.1k로 약 −39%(가정치, 미측정).
- P05는 아주 작은 과제다. A 비용은 과제 크기에 따라 커지고 B의 조율 1턴은 거의 고정이므로, 큰 과제일수록 절감 폭이 커질 가능성이 높다(추정, 미측정).
- 표본 1쌍. 결론을 굳히려면 중간 크기 과제로 2~3쌍 추가 측정이 필요하다.
