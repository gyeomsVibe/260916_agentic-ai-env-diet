# Claude → Codex 72: U31~U35 인계 — 5대 비유 보강 구현, 검증 명령, 판정 요청 (2026-09-25)

- 보낸 쪽: Claude Code(클라우드 세션, Linux·Python 3.11). Codex 부재(PLAN 도구 상태 `codex: ABSENT(USER_DECLARED …)`) 중 사용자 직접 지시로 부지휘자 1회 인수.
- 받는 쪽: Codex(복귀 후 판정자). 브랜치 `claude/cool-hamilton-yj6wwo`, 초안 PR gyeomsVibe/260916_agentic-ai-env-diet#1. `main` 미병합.
- 읽는 순서: 이 메모 → [docs/36 구현 기록](../36_metaphor-realization-implementation-record_2026-09-25.md) → [docs/37 표준 프로세스·하네스](../37_standard-process-and-worker-harness.md) → [docs/35 감사](../35_five-metaphors-realization-audit_2026-09-25.md).
- 사용량: 이 세션의 `/usage` 구독 한도·세션 토큰은 조회할 수 없었다(UNKNOWN). Antigravity·Ollama 호출 0회(이 컨테이너에 미설치).

## 1. 판정 요청 요약 (먼저 볼 것)

| 순위 | 대상 | 요청 | 근거 |
|---|---|---|---|
| 1 | 전체 | **Windows 회귀**: `python .coord/runs/run_regression.py` | 모든 검증이 Linux 기준. Linux 결과: 663개 중 실패 1(B75)·건너뜀 5 |
| 2 | U32a 우편함(Codex 소유 U23 대행 수정) | 무손실 의미(최소 1회 배달 + ID당 한 내용) 채택 여부 | docs/36 §2-1 |
| 3 | U34 계약 매뉴얼 | 형식·검사 규칙을 운영 정본으로 채택할지(구체성 80점, 판정자 분리, cascade 예산) | docs/37 §4 |
| 4 | 범위 위반 판정값 | `SCOPE_VIOLATION`을 BLOCKED가 아닌 REWORK로 둔 설계 | 작업자 산출물 결함이지 환경 장애가 아님. P1 벨 방지 |
| 5 | U33 스트림 자동 보고 기본 켜짐 | 채택 여부 | 과거 테스트 오염 사고 대책: 테스트 패키지 스위치 + PLAN.md 존재 조건 |
| 6 | U35 매뉴얼 3건 | 실행·판정(O1·O2 Ollama, A1 Antigravity) | docs/36 §5 명령 |
| 7 | 사용자 승인 대상 3건 | 사용자에게 승인 요청(예약 도구 deny, 출석 훅, 24/7 등록) | docs/36 §6 |

## 2. 커밋 목록과 직접 검증 명령

모든 코드 커밋은 `Calculator-Exempt`다(이 컨테이너에 Ollama 없음). 이번 변경으로 관문이 `.work/pilot_*`를 인식하므로, 앞으로 정당한 파일럿 결과는 예외가 필요 없다.

| 커밋 | 내용 | 검증 명령 | Linux 결과 |
|---|---|---|---|
| `c0cdd46` | 회귀 테스트가 실제 사용량 장부에 가짜 행을 쓰던 누수 차단, 실행기가 `.coord/usage` 감시 | `python -m unittest tests.test_b24_rejected_summary tests.test_b26_b21_features` + 회귀 후 `runs.jsonl` 미생성 확인 | OK, 수정 되돌리면 `POLLUTED` |
| `b90f6bc` | `coord status`의 스트림 수가 항상 0이던 결함 | `python -m unittest tests.test_cli tests.test_u15_coord_cli` | 10 OK |
| `8ace86f` | 대소문자 구분 FS에서 CLAUDE.md·AGENTS.md 감시 누락, 스트림 참조 드라이브 경로 | `python -m unittest tests.test_b28_reinclude tests.test_u13_isolation tests.test_u15_stream_append tests.test_u18_accept_triage` | OK |
| `353fa4c` | U32a 우편함 무손실 | `python -m unittest tests.test_u32_mailbox_lossless tests.test_u23_mailbox` | 36 OK(신규 14 중 12개 수정 전 실패) |
| `73ce31d` | U32b 교환원·출석부·벨 | `python -m unittest tests.test_u32_sentinel_bell` | 16 OK |
| `8cc0a70` | U33 스트림 자동 보고 기본 켜짐 | `python -m unittest tests.test_u33_pilot_autolog_default tests.test_u15_pilot_autolog` | OK(신규 7개 수정 전 실패) |
| `d5972cb`·`a45b1b9` | U34 계약 매뉴얼·범위 강제·출력 가드·apply·관문 경로·스키마·seed | `python -m unittest tests.test_u34_precision_harness tests.test_u30_olla_evidence tests.test_u22_worker_limits tests.test_u21_calculator` | OK(신규 30개 중 25개 수정 전 실패), U30 고정 인수 SHA `B620…EE2` 불변 |
| `8a0c11f` | U35 POSIX 브로커 소켓이 프로젝트에 남아 파일럿이 멈추던 결함, 매니페스트 비정규 파일 제외 | `python -m unittest tests.test_u35_posix_hygiene` | 3 OK(수정 전 소켓 실패·FIFO 무한 대기) |
| (다음 커밋) | U35-P1 README 정정(apply 파일럿으로 반영), docs/36·37, 매뉴얼 3건, 진입 규칙 한 줄씩, 제안 설정 파일, PLAN·BACKLOG | 아래 §3 | — |

비유 재현(독립 확인용): `python .coord/runs/U31/metaphor_probe.py` → P1~P5 모두 `NOT_REPRODUCED`여야 한다(수정 전 5/5 CONFIRMED).

## 3. 실제 파일럿 1건 (U35-P1) — 새 하네스 전 과정

- 매뉴얼 `.coord/tasks/U35-P1-readme-apply-manual.md`(worker apply, judge claude). 검사 통과 → 1차 실행에서 소켓 결함 발견·수정(`8a0c11f`) → `SUCCEEDED/PASS/DRY_RUN_PASSED`, 토큰 0 → 격리 사본 diff가 매뉴얼의 세 EDIT와 일치함을 직접 대조 → 번들 `e9fcb89b…4d05` 승인 → `APPLIED`.
- 장부 행: `worker=apply`, `model=deterministic`, 토큰 0, `rsi_eligible=false`, `PENDING_INDEPENDENT_VERIFICATION`. 스트림: `20260924T2014-claude-0001`(DRY_RUN_PASSED), `20260924T2015-claude-0001`(APPLIED).
- 판정자와 작성자가 같다(Claude). 이 반영은 Codex 재검토 대상이다.
- 발견: 승인 실행은 장부 행을 남기지 않는다(B78). 매뉴얼의 수치("660")를 인수가 다시 세지 않아, 발행 뒤 테스트가 늘자 틀린 값이 반영됐다. 이 줄은 663으로 직접 정정했다.
- 이 매뉴얼은 이제 `INPUT_HASH_MISMATCH`로 재실행이 거부된다(README가 바뀌었으므로). 정상 동작이다.

## 4. Codex 소유 코드를 대행 수정한 곳 (특히 볼 것)

| 파일 | 원 소유 | 바뀐 것 | 볼 점 |
|---|---|---|---|
| `v7_harness/coord/mailbox.py` | U23 | 임대 시각, 링크 성공 후 삭제, ID당 한 내용(ack·claimed 포함), bad/ 격리, peek, 디렉터리 fsync | Windows에서 `os.link` 실패 시 원본이 claimed에 남는지(의도). 대량 claimed 폴더에서 `_claimed_files` 글롭 비용 |
| `v7_harness/coord/sentinel.py` | U23 | 미정리 판정 조건, 모든 파일럿 폴더, peek, 고착 복구 600초, 커서, 벨 | 600초 임대가 실제 소비자(`codex queue` 60초 제한)에 맞는지 |
| `v7_harness/isolation/security.py` | U13 | 재포함 경로를 디스크 이름으로 대소문자 무시 해석 | Windows 감시 키가 패턴 표기→디스크 표기로 바뀜(판정 불변 예상) |
| `v7_harness/isolation/manifest.py` | U13 | `.coord/presence` 제외, 일반 파일만 포함 | 비정규 파일 제외가 격리 보증을 약화하지 않는지(내용 없음) |
| `v7_harness/broker/ipc.py` | U11 | POSIX 소켓을 임시 폴더로 | Windows 경로 무변경 |
| `v7_harness/pilot.py` | U12~R2 | `allowed_scopes` 전달, `SCOPE_VIOLATION`, 장부 worker 판별 | 요약 키 수(선택 키 `error_detail` 재사용) |

## 5. 알려진 한계·남은 일

- B75: 강제 종료 뒤 같은 주소 재기동 실패는 남음 → U35-A1(Antigravity) 매뉴얼 발행.
- B77: 승인 대상 활성화 3건 미적용. B78: 승인 실행 장부 미기록.
- 삭제 가드는 파이썬 정의(`def`/`class`)만 본다. 문서(.md)의 줄 삭제는 인수 명령(줄 수 확인 등)으로 막아야 한다(U35-O2 매뉴얼 예시).
- 효과(계정 한도 절감)는 `UNMEASURED`. docs/37 §7의 지표로 10표본마다 검토.
- Reddit 현장 근거는 검색 도구 접근 불가로 UNKNOWN.

## 5-1. U35-O1 판정 기대값 (작업자 매뉴얼에는 넣지 않음)

Ollama가 매뉴얼에서 베끼지 않고 증거 문서에서 뽑았는지 보려고, 기대값은 이 메모에만 둔다. 증거 파일 `docs/36…`의 SHA-256은 `fde663da819118581a48e343ddf9fbaf89bf95399bf92e57517126ae2240726a`다.

| 키 | 기대값(docs/36에 정확히 1회) |
|---|---|
| mailbox_tests | `` `tests/test_u32_mailbox_lossless.py` 14개(수정 전 12개 실패) `` |
| sentinel_tests | `` `tests/test_u32_sentinel_bell.py` 16개 `` |
| harness_tests | `` `tests/test_u34_precision_harness.py` 30개(수정 전 코드에서 25개 실패) `` |
| full_suite | `663개(세션 시작 592 + 이번 71) 중 실패 1(B75), 건너뜀 5` |
| u35_bundle | `e9fcb89b…4d05` |

`olla_evidence`가 exit 0이어도 값이 다른 부분 문자열이면(형식은 맞지만 위치가 틀림) 판정은 REWORK다. 다섯 개가 모두 일치한 비율을 로컬 추출 정확도 표본 1건으로 장부에 남긴다.

## 6. Codex가 이 메모를 읽고 할 일(제안 순서)

1. `coord presence --tool codex --state ACTIVE` → `coord inbox`
2. Windows 회귀 → 결과를 PLAN U32~U35 행에 판정으로 기록
3. PR 병합 여부 결정(초안)
4. U35-O1·O2·A1 실행과 판정
5. 사용자에게 B77 승인 요청
