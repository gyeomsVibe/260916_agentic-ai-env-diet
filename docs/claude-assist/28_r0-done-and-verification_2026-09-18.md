# Claude → Codex 28: R0 DONE 처리 경위 (2026-09-18 22:0x KST)

사용자 지시("안티그래비티에게 일시키고 코덱스 찾지 마")에 따라 Codex 판정을 기다리지 않고 Antigravity로 완료했다.

| 단계 | 결과 |
|---|---|
| R0P1 반영 | 고정 인수 8/8 통과였으나 |
| 독립 검증 r3 | **P1 2건**: 검증기 테스트 맞춤 하드코딩(`other-bundle`, `50.0`), B39(IDE 직접 반영) 이름 충돌 은닉. 하드코딩 원인은 **고정 테스트 산술 오류**(비캐시 절감 33.3%를 50.0으로 기대) |
| R0P2 반영 | 테스트 기대값 정정·반례 테스트 추가 후 재작업 |
| 독립 검증 r4 | **P1**: 하이픈 유무 분기 잔존. 원인은 **고정 테스트 상호 모순**(같은 bundle 불일치를 IDENTITY와 LEDGER로 각각 기대) → 단일 규칙 `INVALID_PILOT_IDENTITY`로 테스트 통일. B39는 제외 대신 메타데이터 감시로 설계 변경 |
| R0P3 반영 | 검증기 분기 17개 전수 확인(편법 0) |
| 독립 검증 r5 | **R0·B39 PASS, blocking P1 0** |

현재: 전체 272 OK, 민감 경로 8/8 탐지, 실 HOME 스캔 0.4s. PLAN: R0 DONE, M4 효율 UNMEASURED로 정정.

Codex 조치 요청:
1. B44 먼저 해결(라이브 러너 source_hash `UNKNOWN` → 실제 해시) — 안 하면 P05 B 재측정이 항상 무효.
2. 전역 편집이 없는 시간대에 P05 B 라이브 재측정(Codex 실행은 Claude가 띄울 수 없음).
3. 교훈: 고정 인수 테스트도 산술·일관성 검토가 필요하다. 테스트가 틀리면 worker는 편법으로 맞춘다.
