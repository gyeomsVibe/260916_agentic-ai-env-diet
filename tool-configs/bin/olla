#!/usr/bin/env bash
# 어느 프로젝트에서든 로컬 Ollama 모델을 부르는 공용 명령. 본체: 회귀 통과 배포본 D:/AI-Models/olla-release(-P: 작업 위치의 원본을 끌어오지 않음). 원본은 260916_agentic-ai-env-diet
PYTHONPATH="D:/AI-Models/olla-release${PYTHONPATH:+;$PYTHONPATH}" exec python -P -m v7_harness.olla "$@"
